#!/bin/bash
protected_branches=("main" "master" "develop")
current_branch=$(git branch --show-current)

for protected in "${protected_branches[@]}"; do
  if [ "$current_branch" == "$protected" ]; then
    echo ""
    echo "⚠️ このブランチへの直接プッシュは禁止されています"
    echo ""
    exit 1
  fi
done
